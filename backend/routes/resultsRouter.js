const express = require('express');
const router = express.Router();
const quizHandler = require('../BLL/quizHandler');
const {  } = require('../validation/resultValidation');



module.exports = router;